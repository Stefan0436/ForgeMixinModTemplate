{
    "required": true,
    "minVersion": "${minmixin}",
    "package": "${group}.mixins",
    "refmap": "mixins.${modid}.refmap.json",
    "target": "@env(DEFAULT)",
    "compatibilityLevel": "JAVA_8",
    "mixins": [
    	"ExampleMixin"
    ]
}

